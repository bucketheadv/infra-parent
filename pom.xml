<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.infra.structure</groupId>
        <artifactId>infra-pom</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>infra-pom/pom.xml</relativePath>
    </parent>
    <artifactId>infra-parent</artifactId>
    <packaging>pom</packaging>
    <modules>
        <module>infra-db</module>
        <module>infra-redis</module>
        <module>infra-pom</module>
        <module>infra-core</module>
        <module>infra-job</module>
        <module>infra-web</module>
    </modules>

    <!-- dependencyManagement定义的数据不能通过 parent 标签多重继承 -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.infra.structure</groupId>
                <artifactId>infra-pom</artifactId>
                <scope>import</scope>
                <type>pom</type>
                <version>${infra.structure.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>
</project>
